
#About "find_root_users.sh"
Run as root and in user's root directories (or /opt)
chmod 700 <name script>

#About "audit_protocols.sh"
Only common obsolete protocols have been added to the script. There may be specific protocols that you will have.
